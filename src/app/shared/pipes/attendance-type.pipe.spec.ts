import { AttendanceTypePipe } from './attendance-type.pipe';

describe('AttendanceTypePipe', () => {
  it('create an instance', () => {
    const pipe = new AttendanceTypePipe();
    expect(pipe).toBeTruthy();
  });
});
